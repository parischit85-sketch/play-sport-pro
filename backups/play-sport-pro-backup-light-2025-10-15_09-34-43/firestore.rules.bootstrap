// Firestore Security Rules - BOOTSTRAP MODE
// ATTENZIONE: Queste regole sono molto permissive per il bootstrap iniziale
// DA SOSTITUIRE con firestore.rules dopo il bootstrap

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Durante il bootstrap, permettiamo agli utenti autenticati di creare documenti base
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}