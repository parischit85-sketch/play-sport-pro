// =====================================================
// TEMPORARY: Allow public read/write for verification
// This is ONLY for testing if data exists in database
// =====================================================

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
